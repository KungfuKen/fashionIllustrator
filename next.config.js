/** @type {import('next').NextConfig} */
const nextConfig = {}

// next.config.js
module.exports = {
    images: {
      domains: ['replicate.delivery'], // Add any other allowed domains here
    },
    nextConfig,
  };
  
